import { Mail, Phone, Plus, UsersRound } from "lucide-react";
import { leads } from "../data";
import { StatusBadge } from "../components";
import { SectionPage } from "../section-page";

export default function LeadsPage() { return <SectionPage title="Leads" description="Review and organize every incoming opportunity." icon={UsersRound}><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left"><thead><tr className="border-b border-black/[0.07] text-xs text-[#687080]"><th className="pb-3 font-medium">Lead</th><th className="pb-3 font-medium">Source</th><th className="pb-3 font-medium">Status</th><th className="pb-3 font-medium">Received</th><th className="pb-3 font-medium">Contact</th></tr></thead><tbody>{leads.map((lead, index) => <tr key={lead.name} className="border-b border-black/[0.055] last:border-0"><td className="py-4 text-sm font-semibold">{lead.name}</td><td className="py-4 text-sm text-[#687080]">{lead.source}</td><td className="py-4"><StatusBadge status={lead.status} /></td><td className="py-4 text-sm text-[#687080]">{lead.time}</td><td className="py-4"><button aria-label={`Contact ${lead.name}`} className="grid size-8 place-items-center rounded-lg bg-[#f2f3f5] text-[#687080]">{index % 2 ? <Phone className="size-4" /> : <Mail className="size-4" />}</button></td></tr>)}</tbody></table></div><button className="mt-6 inline-flex items-center gap-2 rounded-lg bg-[#17181c] px-4 py-2.5 text-sm font-medium text-white"><Plus className="size-4" />New lead</button></SectionPage>; }
